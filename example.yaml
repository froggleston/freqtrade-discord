---
token: "your_discord_bot_token_here"

servers:
    - name        : "bot1"
      username    : "user"
      password    : "pass"
      ip          : 127.0.0.1
      port        : 8080
    - name        : "bot2"
      username    : "user"
      password    : "pass"
      ip          : 127.0.0.1
      port        : 8082

disabled_calls:
    - "reload_config"
    - "start"
    - "stop"
    - "forceenter"
    - "forceexit"